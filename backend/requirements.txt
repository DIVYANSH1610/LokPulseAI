fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.1
httpx==0.27.0
python-multipart==0.0.9
python-dotenv==1.0.1
Pillow>=11.0.0

# Optional — only needed if BIGQUERY_PROJECT_ID is set (integrations/bigquery_client.py).
# Requires a GCP billing account enabled.
# pip install google-cloud-bigquery --break-system-packages
# google-cloud-bigquery==3.25.0

# Optional — genuinely free, no account/billing needed. Local Whisper
# transcription (agents/voice_agent.py).
# pip install faster-whisper --break-system-packages
# faster-whisper==1.0.3
